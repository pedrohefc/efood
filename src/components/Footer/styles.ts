import styled from 'styled-components'
import { cores } from '../../styles'

export const FooterContainer = styled.footer`
  background-color: ${cores.brancoTag};
  color: ${cores.rosa};
  text-align: center;
  margin-top: 120px;
`

export const EfoodLogo = styled.img`
  padding: 40px 0 32px;
`
export const Paragraph = styled.p`
  padding: 80px 0 40px;
  font-weight: 400;
  font-size: 10px;
`
